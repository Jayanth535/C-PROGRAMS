#include <stdio.h>

int main() {
   char str[50];
   scanf("%s",str);
   int i; 
   for(i=0;str[i]!='\0';i++)
{
          if(str[i]=='a') 
          str[i]=str[i]-32;
         
          
   }
    printf("%s",str);

    return 0;
}
